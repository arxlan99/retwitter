import Header from 'components/UI/Header';
import React from 'react';

type Props = {};

const List = (props: Props) => {
  return (
    <div>
      <Header title="List" />
    </div>
  );
};

export default List;
