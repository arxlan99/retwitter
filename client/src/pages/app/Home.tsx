import React from 'react';

import WritingCard from 'components/cards/WritingCard';

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <WritingCard />
    </div>
  );
};

export default Home;
